.mb-80
  %h3.in-gold-500.mb-25= t('courses.review.reviews_count', count: course.reviews.size)
  - course.reviews.each do |review|
    .general-message-wrap.divider-lg
      .clearfix
        %img.img-circle.logo-size.inlide-block.pull-left{src: "https://www.smashingmagazine.com/wp-content/uploads/2015/09/smashing-course-5-2-large.jpg"}
        .ml-70
          %div
            .general-message-date.hidden-xs= review.created_at.strftime('%m/%d/%y')
            .max-240
              .general-message-verified= t('review.verified') if review.status == t('review.approved_status')
              .general-message-name= review.name
          .mb-15
            - review.score.times do
              %i.fa.fa-star.rate-star
            - (5 - review.score).times do
              %i.fa.fa-star.rate-empty.fa-lg
          %p= review.review_text
